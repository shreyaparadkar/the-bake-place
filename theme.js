import { DefaultTheme } from 'react-native-paper';

export default theme = {
    ...DefaultTheme,
    roundness: 4,
    colors: {
        ...DefaultTheme.colors,
        primary: '#2a7380',
        accent: '#c791a7',
        surface: '#fe9fc7',
        background: '#fff'
    },
};
